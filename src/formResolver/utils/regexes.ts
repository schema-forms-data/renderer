// Re-export de @schema-forms-data/core — lógica centralizada no pacote core.
export {
    CPF_REGEX,
    TELEFONE_REGEX,
    CEP_REGEX,
    EMAIL_REGEX,
    isValidCpfDigits,
} from '@schema-forms-data/core';
